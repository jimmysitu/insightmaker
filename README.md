InsightMaker Desktop Version
------
# Install
Install nwjs with following commands
```bash
sudo apt-get install nodejs-legacy npm
sudo npm install nw -g --verbose
```
# Run
Run in the root of this repo, which is include package.json
```bash
nw .
```

Enjoy!
